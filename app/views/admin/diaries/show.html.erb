<div class="container">
	<div class="row">
		<div class="col-md-12">
			<%= @diary.title %>
			<%= @diary.body %>
			<%= link_to 'Destroy', admin_diary_path(@diary), method: :delete %>
		</div>
	</div>
</div>